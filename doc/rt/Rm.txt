RMS（速率单调调度）是一种实时任务调度模型，一种基于静态优先级的实时系统调度算法，基于以下假设：1、周期性任务，期限等于周期2、静态优先级，周期短的分配高优先级，以防止不能在期限前调度3、任务不共享会导致其阻塞的资源，一般不实际4、抢占5、上下文切换以及其他人物操作时间例如释放与启动时间为0
RMS模型是一个学术模型，理论上是静态优先级的最优，对于抢占式单处理器是最优的，因为别的静态调度算法可以，它也可以。
DMS（截至期限单调调度）放宽了RMS的第一个条件，期限小于等于周期。
