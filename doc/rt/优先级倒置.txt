当系统中有临界区存在时会发生优先级倒置，当有多任务请求同一临界区，若一个优先级低的占有了该资源，此时高优先级的任务也需要这个资源，就只能等待低优先级任务释放，这时发生优先级逆转，造成不确定延时，因为此时不需要该资源的介于高低优先级之间的任务也会抢占时间。
预防优先级逆转的方法，主要有两种，一是优先级上限，为临界区资源优先级设一上限，当任务占有资源，直接将该任务优先级设为上限，防止被低于最高优先级任务抢占，退出时恢复优先级。但是会造成抢占阻止，阻止低于抢占最高优先级以下的任务。
二是优先级继承，将占有资源的任务的优先级设为请求该资源的最高优先级，更灵活，但是带来更多开销。
